import { HighlightPlayerDirective } from './highlight-player.directive';

describe('HighlightPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPlayerDirective();
    expect(directive).toBeTruthy();
  });
});
